<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_and_ripple_long_item_recycler_view"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imageViewIconCards"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/bg_icon_item_recycler_view"
            android:padding="@dimen/paddingIconItemRecyclerView"
            android:src="@drawable/ic_blue_credit_card" />

        <TextView
            android:id="@+id/textViewShortIdCard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="6dp"
            android:textColor="@color/blackText"
            android:textSize="20sp"
            tools:text="6523" />

        <TextView
            android:id="@+id/textViewBalance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="end"
            android:textColor="@color/blackText"
            android:textSize="20sp"
            tools:text="25,456.00 USD" />

    </LinearLayout>

    <View
        android:id="@+id/viewItemDecorationRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@drawable/decoration_item_recycler_view"
        android:visibility="visible" />

</LinearLayout>